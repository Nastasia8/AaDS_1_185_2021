a = int(input())
arr = list(map(int, input().split()))[:a]
stack = []
index = [0] * a
for i in range(a-1, -1, -1):
    while stack and arr[stack[-1]]>=arr[i]:
        stack.pop()
    index[i] = stack[-1] if stack else -1
    stack.append(i)
print(*index)